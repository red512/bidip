apiVersion: v1
kind: Service
metadata:
  name: {{ include "fe-react.fullname" . }}
  labels:
    {{- include "fe-react.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fe-react.selectorLabels" . | nindent 4 }}
