apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fe-react.fullname" . }}-test-connection"
  labels:
    {{- include "fe-react.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fe-react.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
