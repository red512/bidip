apiVersion: v1
kind: Service
metadata:
  name: {{ include "be-flask.fullname" . }}
  labels:
    {{- include "be-flask.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "be-flask.selectorLabels" . | nindent 4 }}
